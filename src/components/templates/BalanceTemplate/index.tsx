import React from 'react';
import {SingleColumn} from "../../../layout/SingleColumn";
export const BalanceTemplate = ({isMobile}) => {
    return (
        <div>
            <SingleColumn isMobile={isMobile}>
                <div>BalanceTemplate</div>
            </SingleColumn>
        </div>
    );
}
