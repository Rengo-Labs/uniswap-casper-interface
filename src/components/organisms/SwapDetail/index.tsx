import React from 'react';
import {SwapTabs} from 'rengo-ui-kit';
const SwapDetail = () => {
    // TODO we need to get all the information and connect with the components
    return (
        <>
            <SwapTabs firstTokenImg='' secondTokenImg='' />
        </>
    )
}

export default SwapDetail
