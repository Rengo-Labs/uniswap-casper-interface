import styled from 'styled-components'

export const SwapTokenSelectStyled = styled.section`
    box-sizing: border-box;
    width: 100%;
    display: flex;
    justify-content: space-between;
    
`

export const SwapTokenChoiseStyled = styled.aside`
    display: flex;
    place-items: center;
    gap:.2rem;
    &:hover{
        cursor: pointer;
    }
`

export const SwapTokenBalanceStyled = styled.aside`
    display: flex;
    place-items: center;
`
export const SwapTokenRoundedImageStyled = styled.img`
    border-radius: 50%;
`

