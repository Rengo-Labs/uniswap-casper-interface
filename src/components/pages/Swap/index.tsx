import React from 'react';
import Layout from '../../../layout';
import {SwapTemplate} from "../../templates";
export const Swap = () => {
    return (
        <Layout>
            <SwapTemplate />
        </Layout>
    )
};
